# Copie para backend/.env.render e cole a External Database URL do Render (banco qubrix-db).
# Uso local apontando para producao:
#   start-backend.bat render
#   seed-demo.bat render
DATABASE_URL=postgresql://COLE_AQUI_A_EXTERNAL_DATABASE_URL
